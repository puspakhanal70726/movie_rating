<h2>Edit henry</h2>

<%= render "form.html", changeset: @changeset,
                        action: henry_path(@conn, :update, @henry) %>

<span><%= link "Back", to: henry_path(@conn, :index) %></span>
