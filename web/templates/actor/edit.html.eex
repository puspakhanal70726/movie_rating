<h2>Edit actor</h2>

<%= render "form.html", changeset: @changeset,
                        action: actor_path(@conn, :update, @actor) %>

<span><%= link "Back", to: actor_path(@conn, :index) %></span>
